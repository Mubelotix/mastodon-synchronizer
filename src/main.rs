mod config;
use config::*;

fn main() {
    let (doc, config) = read_config("config.toml");
    println!("{:#?}", config);

    // Read config

    // Create venv

    // Install instaloader

    // For each new post:

    // Post image on mastodon

    // Delete image

    // Increase counter in config
}
